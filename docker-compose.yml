# ============================================================================
# TESTERE STORE — Dokploy Compose stack (REPO KÖKÜ)
# Provider = GitHub/Git (Raw DEĞİL) + Compose Path = docker-compose.yml
#
# VERİTABANI: Dokploy'un YÖNETİLEN PostgreSQL + Redis servisleri kullanılır
# (compose içinde gömülü DB YOK). Backend onlara DATABASE_URL / REDIS_URL
# env'leriyle bağlanır → bu değerleri Dokploy UI > Environment'tan ver:
#   DATABASE_URL = postgresql://medusa:...@testerestore-testerepostgres-...:5432/medusa
#   REDIS_URL    = redis://default:...@testerestore-testereredis-...:6379
# Yönetilen DB'ler dokploy-network'te; backend de orada → adları çözülür.
# deploy-marker: v3 (managed-db, force fresh recreate)
# ============================================================================

services:
  backend:
    build:
      context: ./medusa-backend/apps/backend
      dockerfile: ../../../deploy/backend.Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_SECRET: ${COOKIE_SECRET}
      STORE_CORS: ${STORE_CORS}
      ADMIN_CORS: ${ADMIN_CORS}
      AUTH_CORS: ${AUTH_CORS}
      MEDUSA_BACKEND_URL: ${MEDUSA_BACKEND_URL}
      STOREFRONT_URL: ${STOREFRONT_URL}
      RESEND_API_KEY: ${RESEND_API_KEY}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL}
      RESEND_FROM_NAME: ${RESEND_FROM_NAME}
      IYZICO_API_KEY: ${IYZICO_API_KEY}
      IYZICO_SECRET_KEY: ${IYZICO_SECRET_KEY}
      IYZICO_BASE_URL: ${IYZICO_BASE_URL}
      IYZICO_CALLBACK_URL: ${IYZICO_CALLBACK_URL}
    volumes:
      - backend_static:/app/.medusa/server/static
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.testere-api.rule=Host(`${API_DOMAIN}`)"
      - "traefik.http.routers.testere-api.entrypoints=websecure"
      - "traefik.http.routers.testere-api.tls.certResolver=letsencrypt"
      - "traefik.http.services.testere-api.loadbalancer.server.port=9000"

  storefront:
    build:
      context: ./testerestore
      dockerfile: ../deploy/storefront.Dockerfile
      args:
        NEXT_PUBLIC_MEDUSA_BACKEND_URL: ${NEXT_PUBLIC_MEDUSA_BACKEND_URL}
        NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY: ${NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY}
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
        NEXT_PUBLIC_DEFAULT_REGION: ${NEXT_PUBLIC_DEFAULT_REGION}
        NEXT_PUBLIC_DEFAULT_CURRENCY: ${NEXT_PUBLIC_DEFAULT_CURRENCY}
        NEXT_PUBLIC_WHATSAPP_NUMBER: ${NEXT_PUBLIC_WHATSAPP_NUMBER}
        NEXT_PUBLIC_GTM_ID: ${NEXT_PUBLIC_GTM_ID}
        NEXT_PUBLIC_GA_ID: ${NEXT_PUBLIC_GA_ID}
        NEXT_PUBLIC_META_PIXEL_ID: ${NEXT_PUBLIC_META_PIXEL_ID}
        NEXT_PUBLIC_YANDEX_METRIKA_ID: ${NEXT_PUBLIC_YANDEX_METRIKA_ID}
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN}
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      MEDUSA_BACKEND_URL: ${MEDUSA_BACKEND_URL}
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.testere-web.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.testere-web.entrypoints=websecure"
      - "traefik.http.routers.testere-web.tls.certResolver=letsencrypt"
      - "traefik.http.services.testere-web.loadbalancer.server.port=3000"

volumes:
  backend_static:

networks:
  # Dokploy'un Traefik + yönetilen DB ağı (dışarıdan gelir).
  dokploy-network:
    external: true
